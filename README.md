# IRIS macOS validation

Private CI repository for one measured run of `verify_bit_exact(`Qwen/Qwen2.5-0.5B`) on GitHub Actions `macos-latest`. No model weights are stored in the repository; the runner downloads the model at execution time.
